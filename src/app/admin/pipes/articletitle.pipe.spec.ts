import { ArticletitlePipe } from './articletitle.pipe';

describe('ArticletitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticletitlePipe();
    expect(pipe).toBeTruthy();
  });
});
